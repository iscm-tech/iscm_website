import { Metadata } from "next";
import View from "./View";

export const metadata: Metadata = {
  title: "CtrLab — Carbon Transition Lab · MOVE System",
};

export default function page() {
  return <View />;
}
